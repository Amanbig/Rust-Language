fn main() {
    // fn is a keyword used to define a function
    // main is the name of the function that seves as the entry point of the program
    println!("Hello, world!"); // println! is a macro that prints to the console with a newline

    print!("Hello, world!"); // print! is a macro that prints to the console without a newline

}
